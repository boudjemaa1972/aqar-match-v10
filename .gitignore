# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (local secrets — copy from the main checkout, never commit)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# local dev artifacts
dev.log
*.log
*.log.err

# local SQLite database (created by `prisma db push`)
/prisma/db/

# Freebuff desktop app internals — must never be scanned by Tailwind or committed
.freebuff/
